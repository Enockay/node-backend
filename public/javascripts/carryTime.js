let time1;
let amount;
let address;
const carryTime = (timeUnit) => {
  // const { value, unit } = allocatedTime
  time1 = timeUnit;
  // console.log(allocatedTime);
  return { time1 };
};

export default carryTime;
export { time1 };
